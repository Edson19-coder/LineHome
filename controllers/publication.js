const User = require('../models/publication');

function addPublications(req, res) {
    var params = req.body();

    if(params.titlePublication && params.description && params.price && params.location) {
        
    }
}